import SwiftUI

/// Root content view that serves as the app's main entry point into the game.
struct ContentView: View {
    var body: some View {
        GameView()
    }
}

#Preview {
    ContentView()
}
